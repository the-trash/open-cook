-# It's good to be bad!

-# Logick
- @selector_hubs = @hub ? @hub.children.published_set : Hub.sections(@post.ctrl_name)

-# View
- unless @selector_hubs.blank?
  .field
    = label_tag :hub_id, 'Раздел: '
    = select_tag 'post[hub_id]', nested_options(@selector_hubs, selected: @selected_hub), class: :nested_options