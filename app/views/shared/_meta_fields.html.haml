.meta_fields
  %p
    = link_to '#' do
      %b SEO/META
      %i.icon-arrow-down

  .set.hidden
    - %w[ slug author keywords description copyright].each do |name|
      .field
        = f.label t(".#{name}")
        = f.text_field name