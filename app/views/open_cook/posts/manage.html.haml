= render partial: "shared/per_page"

%p= link_to t('posts.new'), url_for([:new, model_name])

%ol.sortable_tree{ data: { max_levels: 5, rebuild_url: url_for([:rebuild, controller_name]) } }
  = sortable_tree @posts

= paginate @posts