- content_for :left_column do
  - if @sub_hubs
    .left_column_hubs
      - @sub_hubs.each do |hub|
        - current = (@hub == hub) ? :current : nil
        %p{ class: current }
          = link_to hub.title, hub_url(hub.slug)
          %i= hub.pubs_count_published